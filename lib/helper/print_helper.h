#ifndef PRINT_HELPER_H
#define PRINT_HELPER_H

#include "../models/models.h"

// Helper function to print Location data
void printLocation(const Location* location);

// Helper function to print Current data
void printCurrent(const Current* current);

// Helper function to print WeatherData
void printWeatherData(const WeatherData* weatherData);

#endif  // PRINT_HELPER_H
